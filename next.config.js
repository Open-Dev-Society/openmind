/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standard Next.js config for OpenMind
  // No special configuration needed — keeping it simple for contributors
};

module.exports = nextConfig;
